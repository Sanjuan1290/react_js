import icon from '../assets/logo.png'

export default function(props){

    return(
        <header>
            <img src={icon} alt="chef claude Logo" />
            <p>Chef Claude</p>
        </header>
    )
}